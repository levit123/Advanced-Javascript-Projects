import {Car} from './classes/car.js'
import {Drone} from './classes/drone.js'
import {fleet} from './fleet-data.js'

//instantiates an object of "Car"
/*
let c = new Car('A123');
console.log(c.licenseNum);
console.log(c.gpsEnabled);
//calls the "start" method through the "Car" class object.
c.start();
//calls the static method in the "Vehicle" base class through the "Car" class,
//which actually does work because "Car" inherits the static method from "Vehicle"
Car.getCompanyName();
*/
console.log(fleet)